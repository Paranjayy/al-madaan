{
  "version": 2,
  "framework": null,
  "installCommand": "bun install",
  "buildCommand": "bun run build && rm -rf .vercel && mkdir -p .vercel/output/functions/__server.func .vercel/output/static && cp -r dist/server/* .vercel/output/functions/__server.func/ && cp -r dist/client/* .vercel/output/static/ && cp dist/config.json .vercel/output/config.json"
}
